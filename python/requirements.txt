# Install torch separately before the rest (CPU-only build, ~200 MB):
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch
silero-vad
vosk
faster-whisper
sounddevice
psutil
openai>=1.0.0
sherpa-onnx
